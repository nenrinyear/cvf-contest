export default function SignInUpLayout({ children }) {
    return (
        <div style={{
            width: '100%',
            height: '100%',
        }}>
            {children}
        </div>
    )
}